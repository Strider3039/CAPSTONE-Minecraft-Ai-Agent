# JVM memory for Gradle (4 GB is safe for Forge builds)
org.gradle.jvmargs=-Xmx4G

# Disable background Gradle daemon (helps avoid weird cache issues in dev)
org.gradle.daemon=false

# Show all warnings during build (helpful for debugging)
org.gradle.warning.mode=all
